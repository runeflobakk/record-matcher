<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.github.runeflobakk</groupId>
        <artifactId>type-companions</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <prerequisites>
        <maven>${maven.target.minimum.version}</maven>
    </prerequisites>

    <artifactId>type-companions-maven-plugin</artifactId>
    <packaging>maven-plugin</packaging>

    <name>Type Companions Maven Plugin</name>
    <description>
        Maven plugin for generating companions for your types, currently with built-in support
        for generating Hamcrest matchers for Java records, and also supports extensions with
        custom code generators.
    </description>

    <properties>
        <maven.plugin-tools.version>3.15.2</maven.plugin-tools.version>
        <maven.target.minimum.version>3.6.3</maven.target.minimum.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-plugin-api</artifactId>
            <version>${maven.target.minimum.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.plugin-tools</groupId>
            <artifactId>maven-plugin-annotations</artifactId>
            <version>${maven.plugin-tools.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-core</artifactId>
            <version>${maven.target.minimum.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.github.runeflobakk</groupId>
            <artifactId>type-companions-generator-spi</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.runeflobakk</groupId>
            <artifactId>record-matcher-generator</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.burningwave</groupId>
            <artifactId>core</artifactId>
            <version>12.66.2</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.quicktheories</groupId>
            <artifactId>quicktheories</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-plugin-plugin</artifactId>
                <version>${maven.plugin-tools.version}</version>
                <executions>
                    <execution>
                        <id>help-goal</id>
                        <goals>
                            <goal>helpmojo</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
